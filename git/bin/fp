#!/bin/bash

# fp: "Feature Push".
# Push the current feature branch.

git status >/dev/null || exit

branch=$(git rev-parse --abbrev-ref HEAD)

if ! printf "%s" "$branch" | grep "^feature/" > /dev/null; then
  printf "Not a feature branch.\n"
  exit 1
fi

git push origin "$branch"

