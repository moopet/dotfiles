#!/bin/bash

# pushup: "Push upstream".
# Pushes a branch if the upstream isn't set yet.

git status >/dev/null || exit

if [ "$#" = "0" ]; then
  branch=$(git rev-parse --abbrev-ref HEAD)
  git push --set-upstream origin "$branch"
else
  printf "Usage: %s\n" "$(basename "$0")"
fi
